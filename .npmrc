# Nx (@nx/nest) and @nestjs/schematics disagree on the prettier peer version,
# which makes a plain `npm install` fail with ERESOLVE. Accept the resolution
# so `npm install` works out of the box in this hybrid Nx/custom repo.
legacy-peer-deps=true
